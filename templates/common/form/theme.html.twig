{% use "form_div_layout.html.twig" %}

{% block form_errors %}
    {% if errors|length > 0 %}
        <div id="{{ id }}_error">
            {%- for error in errors -%}
                {{ error.message }}
            {%- endfor -%}
        </div>
    {% endif %}
{% endblock form_errors %}
